Backend development handles the server-side logic, database, and core functionality of web applications.
Backend is (apne aap me ek) Techloliges ,Backend purely logic programming and terminal base.
2 major components  ==>  programming language(c++,java,js,php) and database(mongo,mysql,sqlite)

business logic => cupan apply or not, credit card is or not ,password exists or not.

two package => Express : Routing (create server) ,, mongo : database.

{
    production pe port ,, process.env.PORT se hi milega otherwise code nahi chalega.
    process.env.PORT ==> kisi server par deploy karte he to server khud ka port deta he jo process.env.PORT me use hota he.
    app.listen(3000) local (your pc) ke liye best he but production pe khali nahi (busy hoga) milega.
    production ==> woh final environment jahan aapki application ya website live hoti hai aur real users use karte hain
}

bundlers ==> all javascript lo or use html,css,js me convert karo (jo browser understood karta he)

CORS error 2 solutions => install cors package

whiteListing is security that preApproved IP-address,users,application ko database / system tak jane ki permission hoti he.

BuildChain/ToolChain ==> (frontend only) if npm run build command ,, all react file le li or ek distribution folder me daldi like...index.html.css ,,

1. domestic and international
2.currat, saving, or jandhan
3.accounts number and secrate pin 4 digit 0r 6 
deposit ,withdraw , currant balance ,